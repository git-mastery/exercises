initialization:
  steps:
    - type: commit
      empty: true
      message: Empty commit
      id: start
    - type: remote
      remote-url: https://github.com/git-mastery/path/part/link-me.git
      remote-name: upstream
